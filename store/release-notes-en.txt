Version 1.3.9 fixes black dynamic website areas in Android exports after device parts are removed or the preview layout changes. Canvas, WebGL, and video frames are now frozen into a capture-safe layer, synchronized with the WebView's committed visual state, exported, and then restored without reloading the website.
