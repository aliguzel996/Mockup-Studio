Version 1.3.4 restores reliable remote website loading on tablets with older Android System WebView versions. The Android preview now uses a compatible fallback bridge, reports a page as ready as soon as it becomes visible, avoids stale cached pages and assets, and keeps the standard browser identity for better CDN compatibility.
