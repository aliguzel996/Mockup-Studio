Version 1.3.3 fixes remote website loading in the Android app by rendering the selected URL in a dedicated native browser surface instead of a restricted iframe. Back, forward, reload, responsive sizing, presentation CSS, and rendered export capture now use the same live preview. The device settings panel also stays inside the workspace with a compact nine-dot drag handle, and the splash logo is fully visible at launch.
