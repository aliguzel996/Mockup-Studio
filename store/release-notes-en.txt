Version 1.3.7 fixes device-part removal on Android by removing the invisible confirmation dialog; parts are now removed directly and can be restored from the top toolbar. Website previews use a standard browser identity, and a failed load closes the native surface and displays the persistent Android WebView error code.
